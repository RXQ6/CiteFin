# Finance Data Thinking

## 项目概览

这是一个面向金融研究、数据分析和风险提示的辅助决策 Agent 项目。系统只提供可追溯的分析与报告，不执行交易、资金划转或账户操作。

## 首次运行

```sh
make setup && make test
```

## 全局硬约束

1. 不执行真实交易、下单、转账、资金划转或账户权限变更。
2. 不承诺收益，不输出保证性结论或未经许可的个性化买卖指令。
3. 所有财务、行情、宏观和公司事实必须标明来源、截至时间与数据口径。
4. 数据缺失、冲突、过期或无法验证时，明确披露不确定性；不得猜测或补造。
5. 计算结论必须可复算；保留输入、方法、参数与结果摘要。
6. 对外回答必须区分事实、推断、情景假设和风险提示。
7. 不得将个人信息、账户信息、密钥或其他敏感数据写入提示词、日志、知识库或输出。
8. 外部工具与数据源遵循最小权限；任何写操作或高影响操作均须用户明确确认。
9. 不得绕过数据治理、来源校验、合规审查或输出安全检查。
10. 原始数据不可直接作为结论依据；先经过清洗、口径确认与质量校验。
11. 重要结论必须提供可定位的证据或引用，不能以模型记忆替代证据。
12. 每次暂停、交接或结束一个可交接工作单元前，必须更新 `docs/PROGRESS.md` 的当前状态、进行中、已知问题与下一步。
13. 会影响架构、口径、模型、工具权限或合规边界的决定，更新 `docs/DECISIONS.md`。
14. 任何测试、数据质量检查或人工复核结果，更新 `docs/VALIDATION.md`。
15. 每完成一个原子、可验证的工作单元后提交 Git 检查点；提交信息必须说明做了什么和为什么，且不得混入无关变更。

## 每次上班

1. 阅读 `docs/PROGRESS.md`、`docs/DECISIONS.md` 与 `docs/VALIDATION.md`（如已存在）。
2. 检查 Git 状态与最近提交，确认未提交改动的归属和当前工作基线。
3. 运行 `make setup && make test`；若失败，先在 `VALIDATION.md` 记录失败事实，再决定是否继续。
4. 从 `PROGRESS.md` 的“下一步”中选择一个原子工作单元；开始前确认其完成标准。

## 每次下班

1. 运行与本次改动对应的测试、校验或人工复核，并记录结果。
2. 更新 `PROGRESS.md`：当前状态、已完成、进行中、已知问题和下一步。
3. 如有重要取舍，更新 `DECISIONS.md`；如有验证结果，更新 `VALIDATION.md`。
4. 检查 Git diff；将一个原子、可验证工作单元提交为检查点。
5. 使用清晰的提交信息：`<type>: <做了什么> — <为什么>`，例如 `docs: add decision log — preserve cross-session design context`。

## 工作状态与跨会话恢复

- 开始工作或上下文不足时，先阅读 `docs/PROGRESS.md`、`docs/DECISIONS.md`、`docs/VALIDATION.md`，再检查 Git 状态。
- `PROGRESS.md` 只记录：当前状态、已完成、进行中、已知问题、下一步与恢复提示。
- `DECISIONS.md` 只记录：决策、原因、备选方案、影响范围与日期。
- `VALIDATION.md` 只记录：执行的检查、结果、失败原因、已知风险与复现方式。
- 不依赖聊天记录作为唯一上下文；以上文件与 Git 检查点是会话重启的事实来源。

## 专题文档导航

- [MVP 产品范围](docs/PRODUCT_SCOPE.md)：判断需求是否属于首期、确认输入输出和完成标准时阅读。
- [数据模型](docs/DATA_MODEL.md)：修改数据库、接口 Schema、证据链、指标或黄金数据时阅读。
- [工作流](docs/WORKFLOW.md)：调整 LangGraph 节点、状态、Hooks、重试、恢复或 Goal Gate 时阅读。
- [功能清单](FEATURES.json)：认领任务、检查依赖、变更状态或提交完成证据时读取。
- [系统架构](docs/architecture.md)：调整 Agent 分层、部署架构或跨服务调用链时阅读；尚待建立。
- [Agent 契约](docs/agent-contracts.md)：新增、修改或交接专业 Agent 时阅读。
- [数据治理](docs/data-governance.md)：接入数据源、定义指标口径、处理数据时效或质量问题时阅读。
- [合规与风险](docs/compliance-and-risk.md)：涉及投资表达、用户风险、权限、敏感信息或人工复核时阅读。
- [工具目录](docs/tool-catalog.md)：新增外部 API、检索工具、计算能力或降级方案时阅读。
- [评测规范](docs/evaluation.md)：修改提示词、模型、工作流或验收标准时阅读。
- [运行维护](docs/operations.md)：处理日志、审计、成本、监控、告警或线上故障时阅读。
- [项目进度](docs/PROGRESS.md)：开始、恢复、暂停或交接任意工作时阅读和更新。
- [决策记录](docs/DECISIONS.md)：需要理解或记录重要取舍时阅读和更新。
- [验证记录](docs/VALIDATION.md)：需要确认当前质量、已知失败项或复现验证时阅读和更新。

## 文档维护原则

将跨项目、不可违反的规则保留在本文件；将业务细节、实现细节和运行记录写入对应专题文档。不要把一次性的任务过程、长篇方案或完整测试输出加入本文件。
